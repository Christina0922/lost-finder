{
  "name": "lostfinder-root",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "start:server": "npm --prefix ./server run start",
    "start:client": "npm --prefix ./client run start",
    "dev": "npm-run-all -p start:server start:client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}

# 서버 실행 가이드

## 서버 시작 방법

### 방법 1: 배치 파일 사용
프로젝트 루트에서 `start-server.bat` 파일을 더블클릭하거나 실행하세요.

### 방법 2: 수동 실행
```bash
cd server
npm start
```

### 방법 3: 개발 모드 (nodemon)
```bash
cd server
npm run dev
```

## 서버 확인
서버가 정상적으로 실행되면 다음 메시지가 표시됩니다:
```
🚀 서버가 포트 5000에서 실행 중입니다.
📡 API 엔드포인트: http://localhost:5000/api
🏥 상태 확인: http://localhost:5000/api/health
```

## 문제 해결

### ECONNREFUSED 에러
- 서버가 실행되지 않았을 때 발생합니다
- 위의 방법으로 서버를 시작하세요

### 포트 5000이 이미 사용 중
- 다른 프로그램이 포트 5000을 사용하고 있을 수 있습니다
- `server/server.js`에서 `PORT` 환경변수로 다른 포트를 설정할 수 있습니다

### 데이터베이스 오류
- SQLite 데이터베이스 파일이 올바른 위치에 있는지 확인하세요
- `server/database.js`를 확인하세요

